# .github/workflows/publish.yml
name: Publish release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - name: Install Bun
        uses: oven-sh/setup-bun@v2
        with:
          registries: |
            https://registry.npmjs.org

      - name: Install dependencies
        run: bun ci
        working-directory: packages/promptyard

      - name: Compile native executables
        working-directory: packages/promptyard
        run: |
          bun run compile-windows-x64
          bun run compile-windows-arm64
          bun run compile-linux-x64
          bun run compile-linux-arm64
          bun run compile-macos-arm64

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: packages/promptyard/dist/*
